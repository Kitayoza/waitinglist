<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Вход в систему | Кабинет пациента</title>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet" />
  <style>
    * { 
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: "Open Sans", sans-serif;
      background: #f4f4f4;
      color: #333;
    }

    header {
      background: #fff;
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid #ccc;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .logo img {
      height: 50px;
    }

    .container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 0 20px;
    }

    /* Auth Form */
    .auth-section {
      display: flex;
      gap: 40px;
    }

    .auth-panel {
      background: white;
      padding: 30px;
      width: 100%;
      max-width: 500px;
      margin: 0 auto;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      border-radius: 5px;
    }

    .auth-panel h1 {
      background: #0097b2;
      color: white;
      padding: 15px;
      margin: -30px -30px 20px -30px;
      border-radius: 5px 5px 0 0;
      font-size: 22px;
      text-align: center;
    }

    .auth-panel .field {
      margin-bottom: 15px;
    }

    .auth-panel label {
      display: block;
      margin-bottom: 5px;
      font-weight: 600;
      color: #555;
    }

    .auth-panel input[type="text"],
    .auth-panel input[type="password"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 3px;
      font-family: inherit;
    }

    .checkbox-field {
      margin: 15px 0;
      display: flex;
      align-items: center;
    }

    .checkbox-field input {
      margin-right: 10px;
    }

    .btn {
      display: inline-block;
      background: #0097b2;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      font-family: inherit;
      font-size: 16px;
      transition: background 0.3s;
    }

    .btn:hover {
      background: #007a8a;
    }

    .btn-primary {
      background: #f7941e;
      width: 100%;
      padding: 12px;
    }

    .btn-primary:hover {
      background: #e07e0c;
    }

    .alert {
      padding: 10px 15px;
      margin-bottom: 20px;
      border-radius: 3px;
    }

    .alert-danger {
      background-color: #f8d7da;
      border-color: #f5c6cb;
      color: #721c24;
    }

    .auth-links {
      text-align: center;
      margin-top: 20px;
      font-size: 14px;
    }

    .auth-links a {
      color: #0097b2;
      text-decoration: none;
    }

    .auth-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">
      <img src="logo.png" alt="Логотип" />
      <div>
        <h2 style="margin: 0; color: #007a87">Кабинет пациента</h2>
        <small>Клиника "ЗДОРОВЬЕ"</small>
      </div>
    </div>
    <div>
      <small>Телефонный центр: (888) 888-88-88</small>
    </div>
  </header>

  <div class="container">
    <div class="auth-section">
      <div class="auth-panel">
        <h1>🔒 Вход в систему</h1>

        <% if flash[:alert] %>
          <div class="alert alert-danger">
            <%= flash[:alert] %>
          </div>
        <% end %>

        <%= form_with url: login_path, local: true do |form| %>
          <div class="field">
            <%= form.label :login, "Логин" %>
            <%= form.text_field :login, class: "form-control", placeholder: "Введите ваш логин" %>
          </div>

          <div class="field">
            <%= form.label :password, "Пароль" %>
            <%= form.password_field :password, class: "form-control", placeholder: "Введите пароль" %>
          </div>

          <div class="checkbox-field">
            <%= form.check_box :remember_me %>
            <%= form.label :remember_me, "Запомнить меня" %>
          </div>

          <div class="field">
            <%= form.submit "Войти", class: "btn btn-primary" %>
          </div>
        <% end %>

        <div class="auth-links">
          <p>Ещё нет аккаунта? <%= link_to 'Зарегистрируйтесь', new_registration_path %></p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>